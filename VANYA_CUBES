#include<iostream>
#include <bits/stdc++.h>
using namespace std;

int sumN (int n)
{
return n*(n+1)/2;}

void maxHeight(int n)
{
long sum=0,i=0;

while (sum<=n)
{
i++;
sum+=sumN(i);

}
cout<<--i;
}

int main(){

ios_base::sync_with_stdio(false);
cin.tie(NULL);

int n;
cin>>n;

maxHeight(n);
}
